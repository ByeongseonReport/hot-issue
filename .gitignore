.deploy_key/
auto-deploy.log
# 작업 스케줄러 내보내기 — 기기명·윈도우 SID가 들어 있다. 공개 저장소라 뺀다.
*.backup.xml

# 편집 전 백업본. 원본이 이미 추적되고 있어 올릴 값어치가 없는데,
# 남겨두면 git status가 매번 「미커밋」으로 띄워 진짜 미커밋을 가린다.
*.bak

# Secrets — NEVER commit these (2026-08-22 보안 감사로 추가)
.env
.env.*
!.env.example
*.pem
*.key
token*.json
client_secret*.json
service-account*.json
credentials*.json
